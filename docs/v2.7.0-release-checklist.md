# v2.7.0 发布检查清单

> 发布日期：2026-08-10
>
> 目标分支：`main`
>
> 发布标签：`v2.7.0`
>
> 支持设备：宽度 `768px` 及以上的平板和电脑

## 1. 工作区与版本

- [x] 当前仓库为 `soapgu/that-math-things`。
- [x] 发布准备基线为 `be55dd5`，开始时 `main` 与 `origin/main` 同步且工作区干净。
- [x] 远端不存在同名 `v2.7.0` 标签或 Release。
- [x] `package.json` 版本为 `2.7.0`。
- [x] `package-lock.json` 根版本和项目版本均为 `2.7.0`。
- [x] 页面与生产构建展示 `v2.7.0`，且没有残留生产 `v2.6.0` 标识。
- [ ] 发布候选提交已推送，且对应主分支 CI 全绿。

## 2. 本地质量门禁

- [x] `npm test -- --run`：37 个测试文件、394 项测试全部通过，耗时 18.28 秒。
- [x] `npm run build`：Vite 转换 5858 个模块并生成生产构建，耗时 4.62 秒。
- [x] `CI=1 npm run test:e2e`：13 个 spec、82 项 Chromium E2E 全部通过。
- [x] `git diff --check`：通过。
- [x] 步骤 7 已完成口诀背诵自动化、浏览器验收和用户体验确认。

## 3. 设备与可访问性矩阵

- [ ] `768×1024`：首页、设置、背诵双表上下排列无横向溢出。
- [ ] `1024×768`：背诵双表上下排列，控制栏和选择可用。
- [ ] `1440×900`：双表左右并排，矩阵尺寸和交互状态正常。
- [ ] `767px`：显示不支持设备提示，不误标为手机端适配。
- [ ] 设置和背诵关键操作可通过键盘完成，焦点与礼貌播报正常。
- [ ] 减少动态效果模式可完成顺序背、自定义背和整表完成。

## 4. CI 与发布

- [ ] 发布候选提交的 GitHub Actions 全部通过，耗时待回填。
- [ ] annotated tag `v2.7.0` 指向通过 CI 的发布候选提交。
- [ ] GitHub Release"v2.7.0 九九乘法口诀背诵"已创建，且非草稿、非预发布。
- [ ] `npm run deploy` 已将发布候选构建部署到 `gh-pages`。
- [ ] GitHub Pages 部署提交 hash 待回填。
- [ ] CDN 资源名与 SHA-256 待回填。

## 5. 发布后线上冒烟

线上地址：<https://soapgu.github.io/that-math-things/>

- [ ] 线上首页已提供新主资源，资源内页面版本为 `2.7.0`。
- [ ] 用户在真实浏览器确认首页显示 `v2.7.0`，且四项顶部导航完整。
- [ ] 首页和顶部导航均可进入"九九乘法"。
- [ ] 设置页"背诵"Tab 可进入口诀背诵页。
- [ ] 顺序背可完成数句并自动领读下一句。
- [ ] 自定义背可选择未完成格并保留反向显示方向。
- [ ] 刷新恢复、重置确认和语音领读正常。
- [ ] 原九九乘法闯关和计算训练入口可用，自动化确认背诵流程不修改 `practice-records` 和闯关数据。
- [ ] `768px`、`1024px` 和 `767px` 支持边界符合版本约定。
- [ ] 线上控制台无未处理 error、React warning 或资源加载失败。

## 6. 发布证据

- 发布候选提交：待回填
- 主分支 CI：待回填
- GitHub Release：待回填
- GitHub Pages 部署提交：待回填
- 线上冒烟时间：待回填
- P1/P2 缺陷：待回填

## 7. Release Notes

正式发布说明见 [v2.7.0 Release Notes](./v2.7.0-release-notes.md)。
