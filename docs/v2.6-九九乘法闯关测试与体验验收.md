# v2.6 九九乘法闯关测试与体验验收

## 1. 验收状态

- 执行日期：2026-08-06
- 基线提交：`4a2f149`
- 自动化状态：✅ 已通过
- 人工体验状态：✅ 用户确认体验正常
- 步骤 7 状态：✅ 已完成

本轮没有更新生产版本号、发布 GitHub Pages 或写入乘法历史数据。步骤 8“文档与发布”成为下一步。

## 2. 自动化结果

| 检查 | 结果 | 证据 |
|---|---|---|
| Vitest | ✅ | 30 个测试文件、315 项测试全部通过 |
| 生产构建 | ✅ | Vite 成功转换 5847 个模块并生成 `build/` |
| Playwright | ✅ | 12 个 spec、73 项 Chromium E2E 全部通过 |
| 乘法专项 E2E | ✅ | 新增 3 个 spec、11 项场景全部通过 |
| 差异格式 | ✅ | `git diff --check` 通过 |

最终完整 Playwright 执行耗时约 55 秒。原有 62 项错题、计算训练、辅助演示、统计和订正场景全部保持通过。

## 3. 覆盖矩阵

### 3.1 难度与题量

| 场景 | 简单 | 提升 | 挑战 |
|---|---:|---:|---:|
| 10 题完整闭环 | ✅ | ✅ | ✅ |
| 提示范围 | 整行整列 16 格 | 实际相邻 2–4 格 | 0 格 |
| 作答方式 | 四选一 | 格内输入、Enter | 格内输入、Enter |
| 正确/错误反馈 | ✅ | ✅ | ✅ |

- 20、50 题：设置、题数、首题、离开保护和刷新回退通过。
- 81 题：在减少动态效果模式下真实完成全部题目；81 个有序坐标无重复、无遗漏，最后一题保持 `FINISHED`，整表点亮后手动进入 100 分、3 星、`81 / 81` 结算。

### 3.2 视口与体验

| 视口 | 难度 | 设置/答题/反馈/结算 | 横向溢出 | 矩阵尺寸稳定 |
|---|---|---|---|---|
| `768×1024` | 简单 | ✅ | ✅ 无 | ✅ |
| `1024×768` | 提升 | ✅ | ✅ 无 | ✅ |
| `1440×900` | 挑战 | ✅ | ✅ 无 | ✅ |

普通动效已验证滑块定位、光束发射、逐格显数、`300ms` 格内替换和 `2s` 自动切题；减少动态效果下三档流程均可完整完成。

### 3.3 无障碍与数据边界

- 设置与答题可用键盘完成，定位结束后焦点进入作答控件，反馈后进入“下一题/查看结果”。
- 离开弹窗默认聚焦“继续闯关”，取消后在弹窗完全关闭时恢复原输入或反馈操作焦点。
- 矩阵包含 10 个 `row`、10 个 `columnheader`、9 个 `rowheader` 和 81 个 `gridcell`。
- 礼貌播报覆盖新题、正确与错误反馈。
- 闯关完成、退出和再来一局前后，`practice-records` 哨兵内容逐字不变。
- 页面没有未处理异常、`console.error` 或 React 警告。

## 4. 验收中发现并修复的问题

| 编号 | 级别 | 问题 | 修复与回归 |
|---|---|---|---|
| MQA-001 | P2 | 浏览器刷新保留 `history.state`，旧会话可能被错误恢复 | 增加刷新文档识别；会话和结算统一 `replace` 回设置页，同一文档可重新开始 |
| MQA-002 | P2 | 取消 Ant Design 离开弹窗后，弹窗自身焦点恢复会覆盖页面焦点 | 改为 `afterClose` 后按原阶段恢复焦点，真实键盘场景通过 |
| MQA-003 | P3 | `RouterProvider` 未接收 future flag，控制台出现 React Router 警告 | 生产入口与组件测试统一传入标志，控制台检查通过 |
| MQA-004 | P3 | 旧响应式用例仍期望 768px 完整品牌，并混用“下一步/下一题” | 对齐已确认的短品牌和实际页面语义，响应式 8/8 通过 |
| MQA-005 | P3 | 辅助提醒答案为 10 时，旧断言把教学阈值“超过了 10”误判为泄露 | 收窄为明确答案语义检查，保留真实防泄露目标 |

当前没有未关闭的 P1、P2 或延期 P3。

## 5. 人工体验结论

用户已按以下清单完成体验，并确认整体流程正常：

1. 在普通动效下完成一局简单 10 题，确认光束节奏、提示显现和自动切题自然。
2. 在提升或挑战模式故意答错一题，确认格内输入痕迹、正确答案和交换律反馈易懂。
3. 在答题中点击顶部导航并取消离开，确认题目、累计矩阵、计时和焦点保持。
4. 完成结算并使用“再来一局”，确认设置保留但题序和累计状态重新开始。
5. 在 768px 与桌面宽度各体验一次，确认矩阵清晰、右侧操作区不拥挤。

人工体验通过，步骤 7 已完成，步骤 8“文档与发布”设为下一步。
