# v2.7 九九乘法口诀背诵测试与体验验收

## 1. 验收状态

- 执行日期：2026-08-10
- 基线提交：`df25831`
- 自动化状态：✅ 已通过
- Chromium可视复核：✅ 已通过
- Firefox人工复核：⏳ 待用户确认
- 用户体验状态：⏳ 待确认
- 步骤7状态：🟡 自动化通过，等待用户体验确认

本步骤没有升级生产版本号、创建标签、发布GitHub Release或部署GitHub Pages。用户确认前不进入步骤8。

## 2. 自动化结果

| 检查 | 结果 | 证据 |
|---|---|---|
| Vitest | ✅ | 37个测试文件、394项测试全部通过 |
| 生产构建 | ✅ | Vite成功转换5858个模块并生成`build/` |
| Playwright | ✅ | 13个spec、82项Chromium E2E全部通过 |
| 背诵专项E2E | ✅ | 新增1个spec、9项场景全部通过 |
| 差异格式 | ✅ | `git diff --check`通过 |

新增背诵Page Object和专项E2E，覆盖统一入口、45句顺序闭环、自定义选择、恢复、重置、异常降级、无障碍、减少动态效果和四档响应式。既有73项错题、计算训练、辅助演示、统计、订正和九九乘法闯关E2E全部保持通过。

## 3. 覆盖矩阵

### 3.1 背诵流程与数据

| 场景 | 结果 |
|---|---|
| 45句传统顺序、跨组推进、无重复遗漏 | ✅ |
| 普通口诀展开两个交换律格，平方口诀展开一个格 | ✅ |
| `9×1`保留显示方向并朗读“一九得九” | ✅ |
| 顺序/自定义切换共享进度并恢复最近模式 | ✅ |
| 刷新恢复完成集合和合法自定义选择 | ✅ |
| 45/45完成45个口诀格、9个标题和81个乘法格 | ✅ |
| 重置取消、确认清空及焦点恢复 | ✅ |
| 损坏存储、写入失败和语音不可用降级 | ✅ |
| 除背诵会话键外的完整localStorage快照不变 | ✅ |

### 3.2 视口与布局

| 视口 | 布局 | 横向溢出 | 关键尺寸 |
|---|---|---|---|
| `768×1024` | 上下排列 | ✅ 无 | 乘法网格保持正方形 |
| `1024×768` | 上下排列 | ✅ 无 | 乘法网格保持正方形 |
| `1440×900` | 左右并排 | ✅ 无 | 控制栏52px，面板573px，网格532px |
| `1920×1080` | 1400px居中并排 | ✅ 无 | 面板573px，网格532px |

可视浏览器抽查`1440×900`完成态：页面`scrollWidth=1440`、`scrollHeight=900`；两面板顶部一致且均为573px，两网格均为532px，控制台无错误或警告。

### 3.3 无障碍与动效

- 两张表均为10行语义结构；乘法表含81个业务格，口诀表含45个口诀格、36个占位格和9个分组标题。
- 顺序背焦点进入当前句和确认按钮；自定义背保持单一Tab停靠点并支持方向键跳过已完成格。
- 重置弹窗默认聚焦“继续保留”，取消后在弹窗关闭完成时恢复触发按钮焦点。
- 礼貌状态区播报当前项、完成进度、完成态和异常，不替代中文学习语音。
- 普通动效逐项断言当前句`220ms`淡入、新格`240ms`展开和完成态`680ms`柔光；减少动态效果下动画与进度过渡关闭，功能保持完整。
- 未背乘积不进入可见文本、属性、辅助名称或隐藏DOM。

## 4. 验收中发现并修复的问题

| 编号 | 级别 | 问题 | 修复与回归 |
|---|---|---|---|
| RQA-001 | P2 | Ant Design弹窗在真实浏览器打开动画后，默认焦点仍可能停留在背景当前口诀 | 改用`afterOpenChange`在打开完成后聚焦“继续保留”，并在关闭完成后恢复触发按钮；组件测试与真实Chromium E2E通过 |

当前自动化范围没有未关闭的P1、P2或延期P3。Firefox兼容性和用户体验确认仍为步骤7关闭门槛。

## 5. Firefox人工复核清单

1. 在`1440×900`确认双表面板与10行分隔线逐行对齐。
2. 确认中文口诀无异常换行、裁切或字体尺寸漂移。
3. 确认已背青绿色、当前蓝色和分组标题完成态清晰。
4. 打开重新开始弹窗，确认默认焦点在“继续保留”，取消后返回触发按钮。
5. 在语音不可用环境确认“我背完了”可立即操作。

## 6. 用户体验确认清单

1. 顺序背完成数句，确认“领读—跟背—确认—下一句”节奏自然。
2. 在自定义背分别点击`1×9`和`9×1`，确认方向与交换律关系直观。
3. 交替使用两种模式，确认共享进度容易理解。
4. 刷新并返回设置后继续，确认恢复结果符合预期。
5. 完成一句后观察两张表同步展开，确认状态清晰。
6. 使用键盘完成自定义选择、确认和重置取消。
7. 在768或1024与桌面宽屏各体验一次，确认布局和触控准确。
8. 体验重新开始确认和45/45完成状态，确认防误清空且庆祝不过度。

用户确认前步骤7保持“自动化通过，等待用户体验确认”。确认并关闭Firefox及体验问题后，步骤7标记完成，步骤8“文档与发布”成为下一步。
