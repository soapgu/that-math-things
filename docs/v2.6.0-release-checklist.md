# v2.6.0 发布检查清单

> 发布日期：2026-08-06
>
> 目标分支：`main`
>
> 发布标签：`v2.6.0`
>
> 支持设备：宽度 `768px` 及以上的平板和电脑

## 1. 工作区与版本

- [x] 当前仓库为 `soapgu/that-math-things`。
- [x] 发布准备基线为 `fcf0731`，开始时 `main` 与 `origin/main` 同步且工作区干净。
- [x] 远端不存在同名 `v2.6.0` 标签或 Release。
- [x] `package.json` 版本为 `2.6.0`。
- [x] `package-lock.json` 根版本和项目版本均为 `2.6.0`。
- [x] 页面与生产构建展示 `v2.6.0`，且没有残留生产 `v2.5.0` 标识。
- [x] 发布候选提交 `a2e108a` 已推送，且对应主分支 CI 全绿。

## 2. 本地质量门禁

- [x] `npm test -- --run`：30 个测试文件、315 项测试全部通过，耗时 17.19 秒。
- [x] `npm run build`：Vite 转换 5847 个模块并生成生产构建，耗时 5.12 秒。
- [x] `CI=1 npm run test:e2e`：12 个 spec、73 项 Chromium E2E 全部通过。
- [x] `git diff --check`：通过。
- [x] 步骤 7 已完成三档难度、四种题量、81 题整表、键盘、播报、减少动态效果和存储隔离验收。

## 3. 设备与可访问性矩阵

- [x] `768×1024`：首页、设置、答题、反馈和结算无横向溢出。
- [x] `1024×768`：矩阵与右侧作答区处于首屏，输入和反馈可用。
- [x] `1440×900`：布局、矩阵尺寸和交互状态正常。
- [x] `767px`：显示不支持设备提示，不误标为手机端适配。
- [x] 设置和答题关键操作可通过键盘完成，焦点与礼貌播报正常。
- [x] 减少动态效果模式可完成三档流程及 81 题整表。

## 4. CI 与发布

- [x] 发布候选提交的 GitHub Actions 全部通过，耗时 3 分 12 秒。
- [x] annotated tag `v2.6.0` 指向通过 CI 的发布候选提交。
- [x] GitHub Release“v2.6.0 九九乘法闯关”已创建，且非草稿、非预发布。
- [x] `npm run deploy` 已将发布候选构建部署到 `gh-pages`。
- [x] GitHub Pages 部署提交为 `b785ab1`；首次平台部署在 10 分钟后超时，失败任务重跑后于 8 分 31 秒成功。
- [x] CDN 已切换到 `assets/index-CwYCTz9-.js`，线上与本地文件 SHA-256 均为 `f72082c48e4429d4a6f70fb873daa7295cee714b05cdc3834216e9aaf65cf5f2`。

## 5. 发布后线上冒烟

线上地址：<https://soapgu.github.io/that-math-things/>

- [x] 线上首页已提供新主资源，资源内页面版本为 `2.6.0`。
- [x] 用户在真实浏览器确认首页显示 `v2.6.0`，且四项顶部导航完整。
- [x] 首页和顶部导航均可进入“九九乘法”。
- [x] 简单模式可完成一局 10 题并进入结算。
- [x] 提升或挑战模式的格内输入、错误反馈和交换律提示正常。
- [x] 81 题设置可选，题量和累计矩阵正常。
- [x] 再来一局、返回设置、离开确认和刷新回退正常。
- [x] 原错题学习和计算训练入口可用，自动化确认乘法流程不修改 `practice-records`。
- [x] `768px`、`1024px` 和 `767px` 支持边界符合版本约定。
- [x] 线上控制台无未处理 error、React warning 或资源加载失败。

## 6. 发布证据

- 发布候选提交：[`a2e108a`](https://github.com/soapgu/that-math-things/commit/a2e108a15e06bd58a16fc4a9bc2fbc39e7ce0cce)
- 主分支 CI：[基础质量检查 #31113118631](https://github.com/soapgu/that-math-things/actions/runs/31113118631)，3 分 12 秒，全部通过
- GitHub Release：[v2.6.0 九九乘法闯关](https://github.com/soapgu/that-math-things/releases/tag/v2.6.0)
- GitHub Pages 部署提交：`b785ab125155f9f2421b2b6085fb80a10e31825d`，[部署工作流 #31113582763](https://github.com/soapgu/that-math-things/actions/runs/31113582763) 重跑成功
- 线上冒烟时间：2026-08-06，用户确认体验正常
- P1/P2 缺陷：无

## 7. Release Notes

正式发布说明见 [v2.6.0 Release Notes](./v2.6.0-release-notes.md)。
